#include <iostream>
#include <set>
using namespace std;
int main() {
    int n;
    while (cin >> n && !(n == 0)) {
        string fentico;
        cin >> fentico;
        set<int> total;
        for (int i = 0; i < fentico.length(); i++) {
            total.insert(id);
        }
    }
}
